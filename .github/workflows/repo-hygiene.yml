name: repo-hygiene

on:
  push:
  pull_request:

jobs:
  verify-repo-hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          lfs: true
      - name: Verify repo hygiene
        run: ./scripts/verify_repo_hygiene.sh
